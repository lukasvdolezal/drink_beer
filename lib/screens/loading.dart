import 'package:flutter/material.dart';

//In case you're reading this, I had too much work on Monday
//and couldn't make this screen in time

class Loading extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
